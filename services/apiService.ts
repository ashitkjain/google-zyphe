/**
 * @deprecated
 * This file is now a thin re-export shim.
 * The implementation has been split into focused modules under `services/api/`.
 *
 * New code should import directly from `services/api` (or specific sub-modules).
 * Existing imports from this path continue to work unchanged.
 */
export * from './api/index';
